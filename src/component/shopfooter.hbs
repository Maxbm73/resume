<footer>
    <hr>
    <div class="row gx-5">

        <div class="col-2 me-4">
            <div class="col-1">
                <ul class="nav">
                    {{#each footer.[0]}}
                    <li class="nav-item">
                        {{#if @last}}
                        <a href="{{link}}" class="nav-link disabled">{{text}}</a>
                        {{else}}
                        <a href="{{link}}" class="nav-link">{{text}}</a>
                        {{/if}}
                    </li>
                    {{/each}}
                </ul>
            </div>
        </div>

        <div class="col-2 me-4">
            <div class="col-1">
                <ul class="nav">
                    {{#each footer.[1]}}
                    <li class="nav-item">
                        {{#if @last}}
                        <a href="{{link}}" class="nav-link disabled">{{text}}</a>
                        {{else}}
                        <a href="{{link}}" class="nav-link">{{text}}</a>
                        {{/if}}
                    </li>
                    {{/each}}
                </ul>
            </div>
        </div>

        <div class="col-2 me-4">
            <div class="col-1">
                <ul class="nav">
                    {{#each footer.[2]}}
                    <li class="nav-item">
                        {{#if @last}}
                        <a href="{{link}}" class="nav-link disabled">{{text}}</a>
                        {{else}}
                        <a href="{{link}}" class="nav-link">{{text}}</a>
                        {{/if}}
                    </li>
                    {{/each}}
                </ul>
            </div>
        </div>

        <div class="col-2 me-4">
            <div class="col-1">
                <ul class="nav">
                    {{#each footer.[3]}}
                    <li class="nav-item">
                        {{#if @last}}
                        <a href="{{link}}" class="nav-link disabled">{{text}}</a>
                        {{else}}
                        <a href="{{link}}" class="nav-link">{{text}}</a>
                        {{/if}}
                    </li>
                    {{/each}}
                </ul>
            </div>
        </div>

        <div class="col-2">
            <div class="col-1">
                <ul class="nav">
                    {{#each footer.[4]}}
                    <li class="nav-item">
                        {{#if @last}}
                        <a href="{{link}}" class="nav-link disabled">{{text}}</a>
                        {{else}}
                        <a href="{{link}}" class="nav-link">{{text}}</a>
                        {{/if}}
                    </li>
                    {{/each}}
                </ul>
            </div>
        </div>


    </div>
</footer>